{
  "author": "Brandon R. Stoner",
  "name": "backbone-authenticate",
  "description": "Exposes a simple authentication interface to your backbone applications.",
  "version": "0.0.1",
  "homepage": "git://source.alytis.net:backbone-authenticate",

  "repository": {
    "type": "git",
    "url": ""
  },

  "engines": {
    "node": "~0.6.10 || 0.8 || 0.9"
  },

  "scripts": {
    "start": "brunch watch --server",
    "test": "brunch test"
  },

  "dependencies": {
    "javascript-brunch": ">= 1.0 < 1.5",
    "coffee-script-brunch": ">= 1.0 < 1.5",

    "css-brunch": ">= 1.0 < 1.5",

    "uglify-js-brunch": ">= 1.0 < 1.5",
    "clean-css-brunch": ">= 1.0 < 1.5"
  },

  "devDependencies": {
  }
}
